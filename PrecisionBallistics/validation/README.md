# Validation reference

`reference_engine.py` is the standalone Python implementation used to validate
the C# solver. Run it with `python3 reference_engine.py` to reproduce the
reference 6.5 Creedmoor trajectory. The C# engine in `../src/PrecisionBallistics.Core`
matches this output to the last decimal.
